<%= form_with(model: flight, local: true) do |form| %>
  <% if flight.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(flight.errors.count, "error") %> prohibited this flight from being saved:</h2>

      <ul>
      <% flight.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :flight_number %>
    <%= form.number_field :flight_number, id: :flight_flight_number %>
  </div>

  <div class="field">
    <%= form.label :airplane_id %>
    <%= form.number_field :airplane_id, id: :flight_airplane_id %>
  </div>

  <div class="field">
    <%= form.label :departure_date %>
    <%= form.datetime_select :departure_date, id: :flight_departure_date %>
  </div>

  <div class="field">
    <%= form.label :origin %>
    <%= form.text_field :origin, id: :flight_origin %>
  </div>

  <div class="field">
    <%= form.label :destination %>
    <%= form.text_field :destination, id: :flight_destination %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
