
// Store the following into variables: number of children, partners name, geographic location, job title. Output your fortune to the screen like so: "You will be a X in Y, and married to Z with N kids."

const numberOfChildren = 8;
const partnerName = 'Criagsy';

const geographicalLocation = 'Glasgow';

const jobTitle = 'chimney sweep';

// const fortune = 'You will be a ' + jobTitle + ' in ' + geographicalLocation + ' and married to ' + partnerName + ' with ' + numberOfChildren + ' kids.';

const fortune = "You will be a ${jobTitle} in ${geographicalLocation} and married to ${partnerName} with ${numberOfChildren}  kids";

const str = `My partner's
"name"


is

${partnerName}`;

console.log( str );
